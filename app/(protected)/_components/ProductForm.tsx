const ProductForm = () => {
  return (
    <div>
      ProductForm for both creation and editing by using a prop called
      type=update or type=create
      Take out the Event Form and utilize it

      1. First work on creating it
      2. Creating product
    </div>
  );
};

export default ProductForm;
